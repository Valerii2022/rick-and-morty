export type FilteredLocationData = {
  name: string;
  type: string;
  dimension: string;
};

export type FilteredEpisodesData = { name: string };

export type FilteredCharacterData = {
  name: string;
  species: string;
  gender: string;
  status: string;
};
